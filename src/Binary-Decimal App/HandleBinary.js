/* const binaryToDecimal = (binaryString) => {
        const converter = parseInt(binaryString, 2)
        setDecimal(converter);
    }
 */
/*    const handleBinary = () => {
        const newBinary = {
            id: binaryString.length,
            binaryString,
            decimal,
        }
        setBinary([...binary, newBinary])
    } */

/*   <Grid item className={classes.spacing}>
                <Button
                    onClick={handleBinary}
                    variant="contained"
                    color="primary"
                >
                    Convert
                </Button>
            </Grid> */
